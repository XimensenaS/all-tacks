#include <cassert>
#include <cmath>
#include <cstdlib>
#include <iostream>
#include <sstream>
#include <vector>
#include <fstream>
#include <ctime>
#include <algorithm>
#include <map>
#include <ctime>
#include <iomanip>

using namespace std;

int main()
{
    string name;
    time_t t = time(nullptr);
    tm *local = localtime(&t);
    vector <string> names;
    vector <tm> date;
    cout << "" << endl;
    cin >> name;
    count =0;
    while (name != "end")
    {
        names.push_back(name);
        cout << "" << endl;
        
    }
}
